# Run the following in a rad repo
# $ git config --add include.path ~/.config/git/rad_gitconfig

[alias]
  patch = push rad -o patch.branch HEAD:refs/patches
  patch-draft = push rad -o patch.branch -o patch.draft HEAD:refs/patches

# vim:ft=gitconfig
